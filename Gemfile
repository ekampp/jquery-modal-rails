source 'https://rubygems.org'

gem 'rails', '4.0.0.rc2'
gem 'sqlite3'

# Assets
# Since 4.0, rails has done away with the assets group
gem 'sass-rails',   '~> 4.0'
gem 'coffee-rails', '~> 4.0'
gem 'jquery-ui-rails'
gem 'jquery-rails'
gem 'uglifier', '>= 1.0.3'

# Specify your gem's dependencies in jquery-modal-rails.gemspec
gemspec

# add rspec
group :test do
  gem 'rspec'
  gem 'rspec-rails'
  gem 'shoulda', :require => false
end
